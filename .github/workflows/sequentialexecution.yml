name: sequential execution
on: workflow_dispatch
jobs:
  job_1_seq_execution:
    runs-on: ubuntu-latest
    steps:
      - name: seq execution job 1
        run: echo " job 1 seq exectuion"
  job_2_seq_execution:
    runs-on: ubuntu-latest
    needs: job_1_seq_execution
    steps:
      - name: seq execution job 2
        run: echo " job 2 seq execution "
