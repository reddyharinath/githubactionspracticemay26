name: sample hello world workflow
on: workflow_dispatch
jobs:
  job_hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: hello world job step
        run: echo "hello world workflow job step"
