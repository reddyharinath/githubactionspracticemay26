name: parallel execution work flow
on: workflow_dispatch
jobs:
  job_1_parallel_execution:
    runs-on: ubuntu-latest
    steps:
      - name: job 1 parallel execution
        run: echo " job 1 parallel execution"
  job_2_parallel_execution:
    runs-on: ubuntu-latest
    steps:
      - name: job 2 parallel execution
        run: echo "job 2 parallel execution"
